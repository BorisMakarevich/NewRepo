" use strict";

let obj = {
    name:'John'
};


console.log(obj.name);



let arr = [1, 2, 3, 4, 5, 6, 7, 3];
//let str = ('I', 'like');
//arr.push('a');
//arr.pop(5);
//arr.shift(1);
//arr.unshift('A');
 //delete arr[2];
//arr.splice([0], 5, 6, 7, 8, 9, 10, "a");
//arr.join(&)
//arr.slice (2, 5)
//arr.sort((a, b) => a - b); //сортирует массив чисел от меньшего к большему
//arr.reverse();
 //arr.concat(8, 9, 10);  //создает новый массив кот прибавляет к старому
 //arr.forEach();
 //arr.map();
 //arr.every();
 //arr.some();
 arr.filter(3);
 //arr.reduce();




 console.log(arr);














